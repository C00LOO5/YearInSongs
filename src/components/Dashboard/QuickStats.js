// src/components/Dashboard/QuickStats.js - Simple Stats Component
import React from 'react';

const QuickStats = ({ data }) => {
  // This is now handled directly in the main Dashboard component
  // This component is kept for compatibility but renders nothing
  return null;
};

export default QuickStats;